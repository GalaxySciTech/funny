# Copy this file to .env (in the same folder as docker-compose.yml) and fill in the values.
JWT_SECRET=change_me_to_a_long_random_string
ALLOWED_ORIGINS=https://your-app.vercel.app

# MongoDB 映射到宿主机的端口（默认 27018，避免与其他项目的 27017 冲突）
MONGO_PORT=27018

# 后端映射到宿主机的端口（默认 5000）
BACKEND_PORT=5000
